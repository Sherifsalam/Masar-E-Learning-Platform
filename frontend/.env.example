# Leave empty in development — Vite proxies /api to the Express server.
# Set this to the deployed API origin (e.g. https://api.masar.app) for builds
# that are not served behind the same proxy.
VITE_API_URL=

# Where the dev proxy forwards /api and /uploads.
VITE_API_PROXY=http://localhost:5000
